{
  "canonicalRuntimeRoot": "apps/web-next/public",
  "canonicalManifestPath": "/manifest.webmanifest",
  "archiveSourceRoot": "web_assets",
  "notes": [
    "All runtime references in the Next app must use public URL paths served from apps/web-next/public.",
    "web_assets is the archive and source export package, not a runtime import target.",
    "Legacy root public and assets folders remain preserved until the final swap and archive phase."
  ]
}
