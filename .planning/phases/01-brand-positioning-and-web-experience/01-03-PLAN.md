---
phase: 01-brand-positioning-and-web-experience
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/config/src/index.ts
  - packages/supabase/src/index.ts
  - apps/web/.env.local.example
  - README.md
  - supabase/README.md
autonomous: true
requirements:
  - WEB-03
  - SECU-05
user_setup: []
must_haves:
  truths:
    - Browser-side configuration uses safe public key terminology and placeholder-only checked-in values
    - Web copy and docs make cloud-backed access boundaries explicit
  artifacts:
    - Shared Supabase helper reflects the safe public client posture
    - Environment examples and docs explain placeholder-only local setup
  key_links:
    - Repo docs, env examples, and client helper behavior all describe the same trust model
---

<objective>
Align frontend configuration, trust messaging, and setup docs with the current safe-client boundary.

Purpose: Make the repo ready for real Supabase credentials later without teaching unsafe patterns or muddying local-only versus cloud-backed expectations.
Output: Clean env examples, aligned shared config, and coherent trust/setup documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-brand-positioning-and-web-experience/01-CONTEXT.md
@packages/config/src/index.ts
@packages/supabase/src/index.ts
@apps/web/.env.local.example
@README.md
@supabase/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten the shared Supabase client boundary</name>
  <files>packages/config/src/index.ts, packages/supabase/src/index.ts</files>
  <read_first>packages/config/src/index.ts, packages/supabase/src/index.ts</read_first>
  <action>Confirm that shared config defaults, placeholder detection, and public client creation consistently use the safe publishable-key terminology, preserve only the minimum compatibility fallback needed, and avoid language that could imply service-role or secret values belong in the client.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - Shared config exports placeholder-safe public client defaults
    - Supabase helper does not introduce secret-key expectations in browser-facing code
  </acceptance_criteria>
  <done>The shared client boundary is explicit and type-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Align env examples and docs with cloud-backed expectations</name>
  <files>apps/web/.env.local.example, README.md, supabase/README.md</files>
  <read_first>.planning/codebase/CONCERNS.md, README.md, supabase/README.md</read_first>
  <action>Update setup docs and env examples so they clearly state that checked-in values are placeholders, that the web surface assumes a cloud-backed Supabase project when real credentials are added, and that sensitive backend secrets belong only in server or CLI deployment contexts.</action>
  <verify>rg -n "publishable|placeholder|cloud backup|required|service role|service-role" README.md supabase/README.md apps/web/.env.local.example</verify>
  <acceptance_criteria>
    - Env example is explicit about placeholder public values
    - Repo docs separate browser-safe values from server-only secrets
  </acceptance_criteria>
  <done>The setup path is clear without exposing unsafe patterns.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm run build:web`
- [ ] Setup docs and shared code all describe the same browser-safe configuration model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Cloud-backed versus local-only expectations are explicit in setup and trust language
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-positioning-and-web-experience/01-03-SUMMARY.md`
</output>
