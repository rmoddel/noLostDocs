---
phase: 08
plan: 01
title: Package naming cleanup
status: complete
files:
  - package.json
  - package-lock.json
  - tsconfig.base.json
  - apps/mobile/
  - apps/web/
  - apps/web-vite-reference/
  - packages/
---

# Plan 08-01

## Goal

Rename the active internal workspace scope from `@doc-wallet/*` to `@nolostdocs/*` and update active code, config, and docs so the product naming is consistent.

## Deliverables

- renamed workspace package names and root scripts
- updated import paths and TS alias mapping
- refreshed lockfile metadata
- updated active planning/docs references
