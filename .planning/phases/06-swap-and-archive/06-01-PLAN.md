---
phase: 06
plan: 01
title: Swap the verified Next app into the live web slot
status: complete
files:
  - apps/web/
  - apps/web-vite-reference/
  - package.json
  - package-lock.json
---

# Plan 06-01

## Goal

Promote the verified Next.js app into `apps/web` and preserve the Vite prototype as `apps/web-vite-reference` so the repo has a live production path and a fallback reference.

## Execution Notes

- move the verified Next app into `apps/web`
- archive the Vite prototype under `apps/web-vite-reference`
- align package names, workspace metadata, and asset-root strings with the live app
- verify the live app still builds cleanly
